import { Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { Button } from '../components/Button';

<Meta title="Components/Button" component={Button} />

# Button

Re-usable button component. For own styling override classes, `button button--primary button--secondary button--tertiary`

- [Props](#props)
- [Code example](#example)

<br />

## Types

<br />

export const Template = args => <Button {...args} />;

### Primary

<Story
  name="Primary"
  args={{
    mode: 'primary',
    children: 'Submit'
  }}
>
  {Template.bind({})}
</Story>

### Secondary

<Story
  name="Secondary"
  args={{
    mode: 'secondary',
    children: 'Cancel'
  }}
>
  {Template.bind({})}
</Story>

### Tertiary

<Story
  name="Tertiary"
  args={{
    mode: 'tertiary',
    children: 'Read more'
  }}
>
  {Template.bind({})}
</Story>

### Loading

<Story
  name="Loading"
  args={{
    mode: 'primary',
    isLoading: true,
    children: 'Submit'
  }}
>
  {Template.bind({})}
</Story>

### Contained

<Story
  name="Contained"
  args={{
    mode: 'primary',
    isContained: true,
    children: 'Submit'
  }}
>
  {Template.bind({})}
</Story>

<br />

### Rounded

<Story
  name="Rounded"
  args={{
    mode: 'rounded',
    children: 'Read more'
  }}
>
  {Template.bind({})}
</Story>

### Ghost

<Story
  name="Ghost"
  args={{
    mode: 'ghost',
    children: 'Read more'
  }}
>
  {Template.bind({})}
</Story>

### Link

<Story
  name="Link"
  args={{
    mode: 'link',
    children: 'Submit'
  }}
>
  {Template.bind({})}
</Story>

### Small

<Story
  name="Small"
  args={{
    mode: 'sm',
    children: 'Submit'
  }}
>
  {Template.bind({})}
</Story>

### Large

<Story
  name="Large"
  args={{
    mode: 'lg',
    children: 'Submit'
  }}
>
  {Template.bind({})}
</Story>

### Reset

<Story
  name="Reset"
  args={{
    mode: 'reset',
    children: 'Cancel'
  }}
>
  {Template.bind({})}
</Story>

## Props

<ArgsTable of={Button} />

<br />

## Example

```jsx
<Button isContained mode="secondary" className="margin-v--lg">
  Cancel
</Button>
```
